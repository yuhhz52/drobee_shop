#server:
#  servlet:
#    context-path: /api
#http://localhost:8080/swagger-ui/index.html#/

# This file is used to configure the application properties
spring:
  application:
    name: shopecom
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shopecom?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 123456
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  #No Sql
  redis:
    host: localhost
    port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: ngthanhhuy09104@gmail.com
    password: ctfr anlo sefq sank
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: {GOOGLE_CLIENT_ID}
            client-secret: {GOOGLE_CLIENT_SECRET}
            redirect-uri: "http://localhost:8080/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            scope:
              - email
              - profile
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo

jwt:
  signerKey: 124bd7af7d0d7d6b60e84f7fbe8c7111284d5ef619e2184f411d47dc50c78f0d
  auth:
    expires_in: 3600

app:
  oauth2:
    redirect-uri: http://localhost:5175/oauth2/callback


#Payment Gateway
stripe:
  secret: {STRIPE_SECRET_KEY}

vnpay:
  tmnCode: L23SI3F3
  hashSecret: NX266PNJEW777PR2N1UZGN3KT4ND6YM4
  payUrl: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
  returnUrl: http://localhost:8080/api/order/vnpay-return

upload:
  dir: uploads






